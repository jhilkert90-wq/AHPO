{
  "config": {
    "step": {
      "user": {
        "title": "Adaptive Hydraulic Pump Optimizer",
        "description": "Select the entities that provide each required signal.",
        "data": {
          "primary_flow_temp": "Primary flow temperature",
          "primary_return_temp": "Primary return temperature",
          "primary_flow_rate": "Primary flow rate",
          "electrical_power_total": "Total electrical power",
          "compressor_frequency": "Compressor frequency",
          "outdoor_temp": "Outdoor temperature",
          "charge_pump_speed": "Charge pump speed (writable)",
          "operating_mode": "Operating mode (optional)",
          "error_status": "Error status (optional)"
        }
      }
    }
  },
  "services": {
    "reset_characteristic_map": {
      "name": "Reset characteristic map",
      "description": "Clears all learned characteristic-map cells."
    },
    "reset_confidence": {
      "name": "Reset confidence",
      "description": "Resets the confidence score of every cell to zero (keeps the learned data)."
    },
    "set_phase_override": {
      "name": "Set phase override",
      "description": "Force Phase A (passive) or Phase B (active) globally, or return to automatic per-cell confidence-based switching.",
      "fields": {
        "phase": {
          "name": "Phase",
          "description": "automatic, passive, or active."
        }
      }
    },
    "export_characteristic_map": {
      "name": "Export characteristic map",
      "description": "Writes the current characteristic map to a JSON file.",
      "fields": {
        "path": {
          "name": "Path",
          "description": "Absolute file path to write."
        }
      }
    }
  }
}
